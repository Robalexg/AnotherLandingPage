import './Nav.css'

const Nav = ({}) => {
    return(
        <nav className='nav--container'>
            <img className='nav--logo' src="../images/logo.svg" alt="" />
        </nav>
    )
}

export default Nav